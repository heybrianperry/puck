<rule_activation id="5ede4571-df21-4777-b143-c5e6686adb83" title="Adopt Snapshot Testing for Component and Reducer Unit Tests: Snapshot Tests Placed" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching test patterns in packages/core/lib and packages/core/reducer.
</rule_activation>

### Rules

- **R-50-001** SHOULD: Snapshot tests SHOULD be placed in __tests__ directories alongside the source files they verify.
- **R-50-007** SHOULD: Use a hybrid approach combining snapshots for structure with targeted assertions for critical values.

### Verify

```bash
# Verify snapshot testing adoption
grep -r 'toMatchSnapshot\|toMatchInlineSnapshot' packages/core/lib/data/__tests__/ packages/core/reducer/actions/__tests__/

# Count snapshot directories
find packages/core -type d -name '__snapshots__' | wc -l

# Run snapshot tests with coverage
npm test -- --coverage --testPathPattern='spec\.(ts|tsx)$'
```

**Accept when:**
- All .spec.ts and .spec.tsx files in packages/core/lib/data/__tests__/ and packages/core/reducer/actions/__tests__/ contain at least one snapshot assertion
- Snapshot directories exist alongside test files and contain .snap files for each test suite
- CI pipeline successfully runs snapshot tests and fails on snapshot mismatches
- Snapshot files are organized in __snapshots__ directories following Jest conventions
- Snapshot serializers are configured for common data types (React elements, dates, etc.)

<enforcement>
Claude Code MUST NOT skip or defer verification. Snapshot testing adoption is verified by automated CI/CD pipeline on every pull request, with mandatory code review of snapshot diffs and pre-commit hooks warning of uncommitted changes. Violations block merge until resolved.
</enforcement>