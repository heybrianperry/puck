<rule_activation id="255f102d-c73a-45cf-af4f-ff000fef4c42" title="Implement Cache Layer for Configuration and Environment Data: Cached Configuration Values" applies_to="**/*">
These rules are ALWAYS ACTIVE for all runtime environments and configuration management systems. All components that handle configuration data or environment variables MUST comply with the caching requirements specified herein.
</rule_activation>

### Rules

- **R-CACHE-001** SHOULD: Cached configuration values SHOULD be immutable within a single request context to prevent race conditions.
- **R-CACHE-002** MUST: Use a singleton pattern or module-level cache to ensure configuration is cached once per application instance.
- **R-CACHE-003** SHOULD: Implement cache warming during application startup to avoid cold start penalties on first requests.
- **R-CACHE-004** SHOULD: Consider using a Map or WeakMap for cache storage to enable efficient lookups and automatic garbage collection.
- **R-CACHE-005** SHOULD: Add instrumentation to track cache hit rates, miss rates, and access patterns for performance monitoring.
- **R-CACHE-006** MUST: Implement strict filtering to prevent caching of sensitive data patterns (EXC-001: security-critical operations like authentication token validation must read directly from environment).
- **R-CACHE-007** SHOULD: Implement cache invalidation hooks triggered by configuration file changes or environment variable updates.
- **R-CACHE-008** SHOULD: Include cache version tracking and automatic refresh mechanisms.
- **R-CACHE-009** SHOULD: Implement cache size limits and eviction policies to prevent memory exhaustion.
- **R-CACHE-010** SHOULD: Monitor cache memory usage with alerts and conduct capacity planning based on expected configuration data volume.

### Scope

In scope:
- Environment variables accessed during request handling
- Application configuration files loaded at runtime
- Feature flags and runtime toggles
- API endpoints and service URLs
- Non-sensitive application settings

Out of scope:
- User session data (covered by separate session management policies)
- Database query results (covered by data layer caching policies)
- Static assets and compiled code
- Real-time streaming data that requires immediate freshness

### Exceptions

- **EXC-001**: Configuration values must be read directly from environment without caching for security-critical operations like authentication token validation.
- **EXC-002**: Development and testing environments may disable caching to facilitate rapid configuration changes.

### Verify

```bash
# Check for cache and config patterns in TypeScript/JavaScript files
grep -r 'cache.*config\|config.*cache' --include='*.ts' --include='*.tsx' --include='*.js' --include='*.jsx' .

# Check for cached environment variable access
grep -r 'process\.env' --include='*.ts' --include='*.tsx' | grep -c 'cache\|memo' || echo 'No cached env access found'

# Run configuration cache tests
npm test -- --grep 'cache.*configuration|configuration.*cache'
```

**Accept when:**
- Configuration access code demonstrates caching mechanism with evidence of in-memory storage
- Cache invalidation or refresh logic is present and tested
- No direct process.env access in hot paths without caching layer
- Performance tests show reduced configuration access latency compared to uncached baseline
- Sensitive data filtering is implemented and verified
- Cache size limits and eviction policies are documented and enforced

<enforcement>
Claude Code MUST verify compliance with these rules during code review and implementation. Automated checks for direct environment variable access in request handlers are mandatory. Performance testing in CI pipeline measuring configuration access latency is required. Manual code review during pull request process is mandatory. Runtime monitoring of cache hit rates and performance metrics must be established. Violations trigger CI pipeline warnings, pull request comments, and performance regression alerts. Exceptions require approval from engineering team lead and security team with documented justification.
</enforcement>