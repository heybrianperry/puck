<rule_activation id="81f8d772-9827-4969-ae57-afc7a8e2bb33" title="Enforce Input Validation and Boundary Checking for Configuration Parameters: Configuration Validation Use" applies_to="**/*">
These rules are ALWAYS ACTIVE for all runtime configuration and environment management code. All configuration parameter handling MUST comply with the input validation rules defined herein.
</rule_activation>

### Rules

- **R-CFG-001** MAY: Configuration validation MAY use schema validation libraries (e.g., Zod, Yup, Joi) to enforce complex validation rules declaratively.
- **R-CFG-002** MUST: Validate all configuration parameters from external sources including environment variables, API requests, user input, and file system operations.
- **R-CFG-003** MUST: Implement validation at the earliest possible point in the data flow, ideally immediately after reading from external sources.
- **R-CFG-004** MUST: Provide clear, actionable error messages that include the parameter name, received value, expected format, and valid range.
- **R-CFG-005** SHOULD: Cache validated configuration values to avoid repeated validation overhead, especially for environment variables that don't change during runtime.
- **R-CFG-006** SHOULD: Log validation failures with appropriate severity levels to enable monitoring and alerting on configuration issues.

### Scope

In scope:
- Environment variable processing and parsing
- API request parameter validation
- User input from forms and interactive elements
- Configuration file parsing (JSON, YAML, TOML, etc.)
- Query string and URL parameter handling
- Runtime feature flags and toggles
- Resource limits and thresholds (memory, timeouts, sizes)

Out of scope:
- Compile-time constants defined in source code
- Type-safe configuration objects created programmatically within the application
- Internal function parameters passed between trusted modules
- Configuration values that have already been validated earlier in the call chain

### Exceptions

- **EXC-001**: Configuration parameters are used in development/testing environments with explicit developer acknowledgment
- **EXC-002**: Performance-critical hot paths where validation overhead is measured to be unacceptable

### Verify

```bash
# Check for unvalidated environment variable access
grep -r 'process\.env\[' --include='*.ts' --include='*.tsx' --include='*.js' | grep -v 'validate\|check\|parse' | wc -l

# Check for unvalidated numeric parsing
grep -r 'parseInt\|parseFloat\|Number(' --include='*.ts' --include='*.tsx' | grep -v 'isNaN\|isFinite\|>\|<\|throw' | head -20

# Run validation-related tests
npm test -- --grep 'validation|config' || echo 'No validation tests found'
```

**Accept when:**
- All direct accesses to process.env are wrapped in validation functions that check type and range
- Numeric parsing operations (parseInt, parseFloat, Number) are followed by boundary checks or NaN/Infinity validation
- Test suite includes validation test cases for configuration parameters covering valid, invalid, and edge case inputs
- Configuration schemas are defined declaratively using schema validation libraries
- Validation failures are logged with appropriate severity levels
- Error messages include parameter name, received value, expected format, and valid range

<enforcement>
Claude Code MUST verify all configuration parameter handling complies with these rules. Static analysis, code review, integration tests, and security scanning are mandatory. Violations in CI pipeline MUST fail the build. Exceptions require tech lead approval and central registry logging.
</enforcement>