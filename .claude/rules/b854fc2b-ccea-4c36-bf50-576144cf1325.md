<rule_activation id="b854fc2b-ccea-4c36-bf50-576144cf1325" title="Adopt Browser-Based Rendering Model for E2E Testing Infrastructure: E2e Test Utilities" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching E2E test utilities and browser-based rendering infrastructure.
</rule_activation>

### Rules

- **R-E2E-001** MUST: E2E test utilities MUST use browser-native rendering APIs to compute element properties and bounding boxes

### Verify

```bash
# Check for browser-native rendering API usage in E2E utilities
grep -r 'getBoundingClientRect\|getComputedStyle' scripts/e2e/utils/ --include='*.mjs' --include='*.js'

# Verify required utility modules exist
test -f scripts/e2e/utils/setup.mjs && test -f scripts/e2e/utils/get-box.mjs

# Run E2E test suite and verify tests pass
npm test -- --grep 'e2e' 2>&1 | grep -q 'passed' || echo 'E2E tests not found or failing'
```

**Accept when:**
- E2E utility modules exist in scripts/e2e/utils/ and use browser-native rendering APIs (getBoundingClientRect, getComputedStyle, etc.)
- Test setup modules initialize browser contexts with consistent configurations before running tests
- E2E test suite passes successfully with rendering-based validations for UI element properties

<enforcement>
Claude Code MUST NOT skip or defer verification. All E2E test utilities must be verified to use browser-native rendering APIs before acceptance.
</enforcement>